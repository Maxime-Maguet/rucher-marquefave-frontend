export default function Home() {
  return (
    <main>
      {/*
       * Hero placeholder — full-viewport height so the footer sits below
       * the first viewport. Implement Hero content here (copy, imagery,
       * CTAs) when ready. The fixed transparent header shows through this
       * full-bleed section at scroll-top.
       */}
      {/* aria-hidden: empty placeholder — no content to announce to screen readers */}
      <section className="min-h-svh" aria-hidden="true" />
    </main>
  );
}
